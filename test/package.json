{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "install-packages": "cd .. && pnpm run repack && cd test && pnpm i --force",
    "test": "pnpm run install-packages && pnpm run vitest",
    "vitest": "export NODE_ENV='emulator' && vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4",
    "vitest": "^0.30.1"
  },
  "dependencies": {
    "@anastasia-labs/direct-offer-offchain": "file:../anastasia-labs-direct-offer-offchain-0.1.0.tgz"
  }
}
